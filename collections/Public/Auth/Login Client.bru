meta {
  name: Login Client
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login/client
  body: json
  auth: none
}

body:json {
  {
    "email": "client1@example.com",
    "password": "password1"
  }
}
