meta {
  name: Login Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login/admin
  body: json
  auth: none
}

body:json {
  {
    "email": "admin1@example.com",
    "password": "password1"
  }
}
